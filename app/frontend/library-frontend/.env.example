# The dev server port is set in vite.config.js, not here.
# This file is kept for anything you want to expose to the browser: Vite only
# passes through variables prefixed with VITE_, reachable as import.meta.env.
#
# VITE_API_BASE=http://localhost:3000
